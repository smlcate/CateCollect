# Folder to watch for incoming files
WATCH_DIR=/absolute/path/to/Incoming

# Backend base URL
API_BASE=http://127.0.0.1:4000

# Optional: if your backend requires auth later
API_TOKEN=

# How to extract the claim number from filenames (first capture group)
# Example matches: "CLM3001_estimate.pdf", "2025-10-07-CLM3001-photos.zip"
CLAIM_REGEX=CLM([0-9]{4,})

# Optional: Windows share style example
# WATCH_DIR=Z:\\Incoming

# HTTPS behavior (keep 0 in dev; set 1 only when behind TLS)
TRUST_HTTPS=0

# Ingest paths (Docker mounts these)
INCOMING_DIR=./data/incoming
ARCHIVE_DIR=./data/archive
