# Server
PORT=3000
NODE_ENV=development

# Postgres
PGHOST=localhost
PGPORT=5432
PGDATABASE=insurance_workflow
PGUSER=workflow_user
PGPASSWORD=devpass

# Paths
STORAGE_ROOT=/path/to/Claims
CCC_EXPORT_WATCH=/path/to/CCC_Exports

# Auth (placeholder)
JWT_SECRET=change_this

# CORS
CORS_ORIGIN=*

# HTTPS behavior (keep 0 in dev; set 1 only when behind TLS)
TRUST_HTTPS=0

# Ingest paths (Docker mounts these)
INCOMING_DIR=./data/incoming
ARCHIVE_DIR=./data/archive
